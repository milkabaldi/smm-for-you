---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
---

<BaseLayout title="PUSH — SMM Agency" description="Стильний SMM для бізнесу">
  <main id="top">
    <Header />

    <section class="hero">
      <div class="container hero__inner">
        <div class="hero__left">
          <h1 class="hero__title">
            СТИЛЬНИЙ<br />
            SMMM ДЛЯ ТЕБЕ
          </h1>
        </div>

        <div class="hero__right">
          <p class="hero__text">
            Ми — Push Agency, креативне агентство для соцмереж, яке поєднує системний підхід
            і нестандартні ідеї. Тут ти знайдеш сучасний дизайн, зрозумілу навігацію і рішення,
            які реально працюють.
          </p>

          <a class="btn" href="#contact">
            Зв’язатись з нами
            <span class="btn__icon">↗</span>
          </a>
        </div>
      </div>

      <!-- декоративні "куби" (плейсхолдери під твої PNG/SVG) -->
      <div class="floaters" aria-hidden="true">
        <div class="cube c1">IG</div>
        <div class="cube c2">in</div>
        <div class="cube c3">TT</div>
        <div class="cube c4">YT</div>
        <div class="cube c5">f</div>
        <div class="cube c6">X</div>
      </div>
    </section>

    <!-- секції-плейсхолдери, щоб навігація працювала -->
    <section id="services" class="section container"><h2>Наші Кращі Послуги</h2></section>
    <section id="why" class="section container"><h2>Чому ми?</h2></section>
    <section id="cases" class="section container"><h2>Кейси</h2></section>
    <section id="actors" class="section container"><h2>Актори</h2></section>
    <section id="process" class="section container"><h2>Етапи співпраці</h2></section>
    <section id="faq" class="section container"><h2>FAQ</h2></section>

    <section id="contact" class="section container">
      <h2>Контакти</h2>
      <p style="opacity:.85">Сюди вставимо форму/телеграм/емейл.</p>
    </section>

    <footer class="footer container">
      <div class="footer__card pill">
        <strong>PUSH</strong> • © {new Date().getFullYear()} • SMM & Content
      </div>
    </footer>
  </main>

  <style>
    .hero{
      position:relative;
      padding: 46px 0 38px;
      min-height: 78vh;
      display:flex;
      align-items:center;
    }

    .hero__inner{
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      align-items:center;
      gap: 34px;
      position:relative;
      z-index:2;
    }

    .hero__title{
      margin:0;
      font-size: clamp(44px, 6.2vw, 92px);
      line-height: .9;
      font-weight: 1000;
      letter-spacing: .5px;
      text-transform: uppercase;
      text-shadow: 0 18px 50px rgba(0,0,0,.28);
    }

    .hero__text{
      margin:0 0 18px 0;
      max-width: 520px;
      color: var(--muted);
      font-size: 15px;
      line-height: 1.55;
    }

    .floaters{
      position:absolute;
      inset:0;
      overflow:hidden;
      z-index:1;
      pointer-events:none;
      filter: drop-shadow(0 25px 40px rgba(0,0,0,.35));
    }

    .cube{
      position:absolute;
      width: 120px;
      height: 120px;
      border-radius: 26px;
      background: rgba(0,0,0,.62);
      border: 1px solid rgba(255,255,255,.08);
      display:grid;
      place-items:center;
      font-weight: 900;
      letter-spacing:.5px;
      color: rgba(255,160,40,.95);
      transform: rotate(-12deg);
      box-shadow: 0 18px 40px rgba(0,0,0,.35);
      backdrop-filter: blur(6px);
    }
    .cube::after{
      content:"";
      position:absolute;
      inset: 16px;
      border-radius: 18px;
      background: radial-gradient(circle at 30% 30%, rgba(255,140,30,.95), rgba(255,80,0,.25) 60%, transparent 70%);
      opacity: .65;
      filter: blur(.2px);
    }
    .cube{ font-size: 28px; }

    .c1{ left: 6%; top: 18%; transform: rotate(-18deg); }
    .c2{ right: 5%; top: 28%; transform: rotate(14deg); }
    .c3{ left: 10%; bottom: 16%; transform: rotate(22deg) scale(1.15); }
    .c4{ right: 18%; bottom: 10%; transform: rotate(-8deg) scale(.95); }
    .c5{ left: 9%; top: 46%; transform: rotate(-6deg) scale(.8); opacity:.9; }
    .c6{ right: 7%; top: 12%; transform: rotate(10deg) scale(.75); opacity:.85; }

    .section{
      padding: 70px 0;
    }
    .section h2{
      margin:0;
      font-size: 28px;
      text-shadow: 0 18px 50px rgba(0,0,0,.18);
    }

    .footer{ padding: 28px 0 40px; }
    .footer__card{ padding: 18px 16px; text-align:center; }

    @media (max-width: 980px){
      .hero{ min-height: auto; padding: 34px 0 24px; }
      .hero__inner{ grid-template-columns: 1fr; }
      .hero__right{ max-width: 620px; }
      .cube{ width: 92px; height: 92px; border-radius: 22px; font-size: 22px; }
    }
  </style>
</BaseLayout>
